package com.example.feedcraft.activityEditor

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.example.feedcraft.R

class EditorActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_editor)



//        val bundle = intent.getBundleExtra("bundle")

//        if(bundle != null) {
//            val fragment = EditFragment()
//            fragment.arguments = bundle
//
//            val fragmentManager = supportFragmentManager
//            fragmentManager.beginTransaction().replace(R.id.editor_fragment_host, fragment).commit()
//        }





        /*val binding = ActivityEditorBinding.inflate(layoutInflater)
        setContentView(binding.root)*/




    }

}